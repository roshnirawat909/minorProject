

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>BloggifyHub | All Posts</title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    /* Custom CSS for uniform card image heights */
    .card-img-top-fixed {
      height: 200px;
      object-fit: cover;
    }
  </style>
</head>

<body>

  <div class="container py-4">

    <div class="d-flex justify-content-end mb-4">
      <a href="/posts" class="btn btn-success">Create New Post</a>
    </div>

    <h1 class="mb-4 text-center fw-bold">All Posts</h1>

    <% if (allPosts.length === 0) { %>
      <p class="text-center text-muted">No posts available yet.</p>
    <% } %>

    <div class="row">

      <% allPosts.forEach(post => { %>
        <div class="col-md-6 col-lg-4 mb-4">

          <div class="card shadow-sm rounded">

            <img src="<%= post.image %>" class="card-img-top card-img-top-fixed" alt="Post Image">

            <div class="card-body">

              <span class="badge bg-primary"><%= post.category %></span>

              <h5 class="mt-3 fw-bold">
                <a href="/posts/<%= post._id %>" class="text-dark text-decoration-none">
                  <%= post.title %>
                </a>
              </h5>

              <p class="text-muted">
                <%= post.body ? post.body.substring(0, 120) : "" %>...
              </p>

              <p class="small text-secondary">
                By <strong><%= post.author || "Anonymous" %></strong> ·
                <%= post.createdAt ? new Date(post.createdAt).toLocaleDateString() : "" %>
              </p>

              <a href="/posts/<%= post._id %>" class="btn btn-primary w-100">Read More</a>

            </div>
          </div>
        </div>
      <% }) %>

    </div>
  </div>

</body>
</html>